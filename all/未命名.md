```mermaid
graph TD
    %% 核心技术栈
    subgraph 核心技术栈
        subgraph 后端框架
            A1[Spring Boot 2.6.2] --> A2[核心框架]
            B1[Spring Cloud 2021.0.0] --> B2[微服务架构]
            C1[Spring Cloud Alibaba Nacos 2021.0.5.0] --> C2[服务注册发现 & 配置中心]
            D1[OpenFeign] --> D2[声明式服务调用]
            E1[Spring Cloud LoadBalancer] --> E2[客户端负载均衡]
        end

        subgraph 数据持久化
            F1[MyBatis 1.3.2] --> F2[ORM框架，39个Mapper映射]
            G1[MySQL 8.0.13] --> G2[关系型数据库]
            H1[Druid 1.2.8] --> H2[数据库连接池 & SQL监控]
            I1[Sharding-JDBC 3.1.0] --> I2[分库分表中间件]
            J1[PageHelper 1.2.5] --> J2[分页插件]
        end

        subgraph 安全与认证
            K1[Apache Shiro 1.13.0] --> K2[安全框架（认证&授权）]
            L1[EhCache 2.10.5] --> L2[缓存框架（Session缓存）]
            M1[JWT (jjwt 0.9.0)] --> M2[Token认证]
            N1[Spring Security Crypto 5.6.1] --> N2[密码加密（Argon2）]
            O1[BouncyCastle 1.68] --> O2[加密算法库]
            P1[自定义加密服务] --> P2[RSA + AES双重加密]
        end

        subgraph 视图层
            Q1[Thymeleaf] --> Q2[服务端模板引擎]
            R1[Nekohtml] --> R2[HTML解析]
            S1[Thymeleaf-Extras-Shiro 2.0.0] --> S2[Shiro模板整合]
        end

        subgraph 任务调度
            T1[Quartz 2.3.0] --> T2[定时任务调度框架]
            U1[18个Job任务模块] --> U2[业务定时任务]
        end
    end

    %% 第三方集成
    subgraph 第三方集成
        V1[支付宝 SDK 3.7.4] --> V2[支付宝支付]
        W1[微信支付 SDK] --> W2[微信支付 & 人脸支付]
        X1[百度 AI SDK 4.12.0] --> X2[OCR识别]
        Y1[银行接口] --> Y2[ABC、CCB等多家银行对接]
    end

    %% 工具库
    subgraph 工具库
        Z1[Hutool 5.3.3] --> Z2[Java工具类库]
        AA1[Fastjson 1.2.47] --> AA2[JSON解析]
        AB1[Gson] --> AB2[JSON解析]
        AC1[Apache Commons] --> AC2[通用工具<br>Lang3/IO/FileUpload/Compress]
        AD1[Apache POI 3.9] --> AD2[Excel处理]
        AE1[ZXing 3.3.0] --> AE2[二维码生成]
        AF1[Thumbnailator 0.4.8] --> AF2[图片压缩处理]
        AG1[Metadata-Extractor 2.7.0] --> AG2[图片元数据提取]
    end

    %% 其他模块
    subgraph 网络与通信
        AH1[Netty 4.1.19] --> AH2[高性能网络框架]
        AI1[OpenFeign] --> AI2[微服务间HTTP调用]
    end

    subgraph 验证码与安全
        AJ1[Kaptcha 2.3.2] --> AJ2[验证码生成]
        AK1[自定义验证码文本生成器] --> AK2[中文验证码支持]
    end

    subgraph 监控与日志
        AL1[Spring Boot Actuator] --> AL2[应用监控 & 健康检查]
        AM1[Logback] --> AM2[日志框架]
        AN1[Druid监控] --> AN2[SQL性能监控]
        AO1[UserAgentUtils 1.19] --> AO2[客户端信息解析]
    end

    subgraph 开发工具
        AP1[Lombok 1.16.18] --> AP2[简化Java代码]
        AQ1[Spring Boot DevTools] --> AQ2[热部署]
        AR1[Velocity 1.7] --> AR2[代码生成模板]
        AS1[Swagger] --> AS2[API文档（可选）]
    end

    %% 架构特点
    subgraph 架构特点
        AT1[微服务架构] --> AT2[基于Spring Cloud Alibaba]
        AU1[分布式部署] --> AU2[Nacos多环境配置]
        AV1[数据分片] --> AV2[Sharding-JDBC分库分表]
        AW1[高安全性] --> AW2[Shiro+JWT+RSA/AES]
        AX1[高性能] --> AX2[Druid+EhCache+Netty]
        AY1[可扩展] --> AY2[53个Controller/51个Service]
        AZ1[多端支持] --> AZ2[Web端+移动端]
        BA1[丰富集成] --> BA2[支付/OCR/银行接口]
    end

    %% 业务模块
    subgraph 业务模块
        BB1[卡务管理] --> BB2[发卡/挂失/补办/换卡]
        BC1[消费管理] --> BC2[消费记录/退款/补贴]
        BD1[支付管理] --> BD2[支付宝/微信/银联/人脸支付]
        BE1[订单管理] --> BE2[订餐/商品订单]
        BF1[设备管理] --> BF2[门禁/水控/热泵设备]
        BG1[报表统计] --> BG2[消费报表/财务报表]
        BH1[用户管理] --> BH2[档案管理/权限管理]
    end

    %% 关联关系
    classDef box fill:#f9f9f9,stroke:#333,stroke-width:1px;
    class 后端框架,数据持久化,安全与认证,视图层,任务调度,第三方集成,工具库,网络与通信,验证码与安全,监控与日志,开发工具,架构特点,业务模块 box;
```